# Open Studio

Open:

```text
http://127.0.0.1:8780
```

Expected flow:

```text
Gateway request -> ActionIR / ActionGraph -> evidence extraction -> PolicyDecision -> AuditLog / EvidenceGraph -> Studio timeline
```
